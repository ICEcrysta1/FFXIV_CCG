## 项目架构

```text
FFXIV_CCG/
├── .agents/                                          Codex/agent 本地辅助目录，不承载项目业务代码
├── .codex/                                           Codex 本地运行目录，存放会话和本地辅助数据，不属于项目实现
├── .git/                                             Git 元数据目录，记录版本历史、分支和对象数据，不手动修改内部内容
├── .pytest_cache/                                    pytest 默认缓存目录，属于测试副产物
├── .tmp/                                             临时输出目录，当前主要放 pytest 缓存和状态机示例导出文件
├── .venv/                                            本地 Python 虚拟环境目录，不承载项目业务代码
├── .vscode/                                          VS Code 本地工作区配置目录，不属于项目实现
├── .zcode/                                           本地辅助运行目录，存放 plans 等本地辅助数据，不属于项目实现
├── artifacts/                                        训练运行时产物，自动生成不手动维护
│   ├── checkpoints/                                  训练中间检查点（断点续训用）
│   ├── exports/                                      导出文件（ONNX/TorchScript 等部署格式）
│   └── model_analysis/                               科研模型分析 PNG 与 metadata 输出
├── Combat.Sim/                                       C# 状态机工程（net10.0）；职业/系统 YAML 配置权威来源不变，C# 直接复用 config/ 下配置
│   ├── FightEngine/                                  C# 状态机核心类库（最终 FightEngine.dll 产物，供游戏插件引用），按 Models/Common/System/Jobs/Skills/Config/Outputs/Facade 分层
│   │   ├── Models/                                   数据 DTO 目录
│   │   │   ├── Combat/                               战斗状态运行时值（可变 class + Clone）
│   │   │   │   ├── ActionHistoryEntry.cs             单次动作历史快照（含 request/cast/effect 时间与 ActionInstanceId；state_before/state_after 为输出层 StateContext 产物）
│   │   │   │   ├── CooldownState.cs                  冷却桶：可用充能/绝对回充时刻，剩余时间按观测派生
│   │   │   │   ├── CombatState.cs                    战斗快照：玩家标量/量谱/职业时间资源绝对截止时刻/冷却/Buff/DoT/历史 + Clone(copyHistory)
│   │   │   │   ├── DotState.cs                       目标 DoT 运行时值：绝对过期/下次 tick 时刻
│   │   │   │   ├── StatusState.cs                    Buff 运行时值：绝对过期时刻与派生剩余时间
│   │   │   │   └── ValidationResult.cs               动作合法性检查结果
│   │   │   ├── Definitions/                          静态定义（record）
│   │   │   │   ├── ActionKind.cs                     技能类型枚举：GCD / oGCD
│   │   │   │   ├── JobResourceDefinition.cs          职业量谱定义：类型/默认值/向量分组/显示单位/上限
│   │   │   │   ├── JobStateRegistration.cs           职业向系统层注册的资源与状态定义
│   │   │   │   ├── SkillDefinition.cs                技能静态定义：威力/训练价值/耗蓝/full 语义/冷却/充能/dot/AoE 衰减；可选集合统一为空集合
│   │   │   │   └── StatusDefinition.cs               状态静态定义：game_id/时长/最大层数
│   │   │   ├── Policy/                               模型控制动作与纯策略决策 DTO
│   │   │   │   ├── PolicyActionDefinition.cs         不进入 SkillBook 的 policy 候选定义
│   │   │   │   └── PolicyDecision.cs                 带时间戳及前后观测快照的纯策略决策
│   │   │   └── Timeline/                             时间线事件、mutation 与完整模拟快照
│   │   │       ├── ActionLifecyclePayload.cs         动作实例在接受、读条完成和效果事件间传递的可深拷贝载荷
│   │   │       ├── ActionRequest.cs                  仅携带绝对 Timestamp 与 SkillKey 的动作请求
│   │   │       ├── ActionSubmissionResult.cs         不夹带状态的轻量动作结果：接受/排队/reason、实例 id 与关键时间
│   │   │       ├── ActionTimingPlan.cs               动作接受后固定使用的完整读条锁、服务器提前效果延迟、GCD 和占用时序计划
│   │   │       ├── ExternalCombatEvent.cs            带绝对时间戳的 Boss/移动/目标数/团辅类型化外部事实与稳定事件键
│   │   │       ├── ExternalEventResult.cs            不夹带状态的轻量外部事实提交结果
│   │   │       ├── ITimelineEventPayload.cs          需要随 timeline snapshot 深拷贝的事件载荷契约
│   │   │       ├── SimulationSnapshot.cs             CombatState、待处理事件、待结算事实和 sequence 游标的可克隆快照
│   │   │       ├── TimelineEvent.cs                  事件类型、稳定优先级、绝对时间和 sequence
│   │   │       ├── TimelineEventOrder.cs             事件与待结算事实共用的派发排序键（时间→优先级→sequence）
│   │   │       └── TimelineMutation.cs               领域处理器返回的状态变更与后续事件请求
│   │   ├── Common/                                   公共层目录
│   │   │   ├── GcdUnits.cs                           秒数与 GCD 数量安全换算
│   │   │   ├── RepoRootLocator.cs                    仓库根目录定位（向上找含 config/default.yaml 的目录）；缺省 projectRoot 的构造路径与宿主入口复用
│   │   │   ├── SceneContracts.cs                     scene 契约常量：窗口 context key/绝对时间模式/统一滑步窗口/场景浮点容差（定义由 config/schema.yaml 提供，对照 common/contracts.py）
│   │   │   ├── SceneWindow.cs                        scene window feature key 索引映射缓存与窗口索引解析
│   │   │   ├── YamlConfig.cs                         YAML mapping 加载：YamlDotNet 解析树 + Plain 标量类型提升（null→bool→int→float→string，数值统一 double）；前导零整数显式拒绝防静默错值
│   │   │   └── YamlValues.cs                         Python 语义配置值转换：int 截断/float/bool/dict.get/相对路径解析
│   │   ├── System/                                   系统状态机行为层
│   │   │   ├── Registries/                           注册中心
│   │   │   │   ├── BuffStateRegistry.cs              Buff 注册：系统/职业定义、grant_status 校验与应用、授予/清除/层数消耗
│   │   │   │   └── ResourceStateRegistry.cs          量谱注册：上限覆盖、归一化钳制、快照 round4 与前后差分
│   │   │   ├── Timeline/                             公共时间轴推进
│   │   │   │   ├── CombatTimelineRuntime.cs          唯一时间推进入口：绝对时钟、公共资源事件队列、稳定排序、已到期资源事件的保留、重复派发守卫、snapshot/fork
│   │   │   │   ├── CooldownRuntime.cs                冷却桶：充能校验/消耗/绝对回充事件/主动减少/快照
│   │   │   │   ├── DotTimelineRuntime.cs             DoT 注册/挂载/单次 tick 事件结算，不独立推进时间
│   │   │   │   ├── JobTimelineRegistry.cs            职业周期资源与倒计时的绝对截止时刻声明、到期事件处理、MP tick modifier 注册与对外计时器视图投影，不提供按秒推进
│   │   │   │   ├── PendingSettlementQueue.cs         待结算队列：承接已越过到期点、脱离资源重新描述的到期事实，与事件队列共用派发排序键，按目标去重
│   │   │   │   ├── StatusTimelineRuntime.cs          Buff 授予/刷新/单次过期事件处理，持有职业状态到期钩子并在移除状态前调用，不独立推进时间
│   │   │   │   └── JobTimerProjection.cs             职业计时器绝对截止时刻到模型相对秒数视图的单向输出投影
│   │   │   ├── HistoryRuntime.cs                     动作历史记录 + 量谱差分挂载 + 职业计时器投影 + 长度上限
│   │   │   ├── MpRecoveryRuntime.cs                  自然回蓝 tick 事件规则与职业 MP modifier，不独立推进时间
│   │   │   ├── PlayerStateRuntime.cs                 玩家公共态：读条/GCD/weave/停手及公共校验；读条与 GCD 锁由绝对截止时刻派生，oGCD 间隔由调用方调度
│   │   │   ├── SystemConstants.cs                    ZERO_EPSILON = 0.0001
│   │   │   └── SystemStateMachine.cs                 系统规则装配：公共领域处理器、时间线构建、外部事实预校验、威力解析与职业量谱输出投影
│   │   ├── Jobs/                                     职业子状态机公共辅助、契约、注册中心与真实职业实现
│   │   │   ├── Black.Mage/                           黑魔职业子状态机目录
│   │   │   │   ├── BlackMageApplications.cs          黑魔行为应用：技能效果、stance 过渡、灵极魂/回蓝/通晓资源辅助
│   │   │   │   ├── BlackMageConstants.cs             黑魔职业常量表：火冰极性威力倍率、AF 耗蓝倍率/灵极魂折扣、醒梦回蓝、灵极魂回蓝
│   │   │   │   ├── BlackMageElemental.cs             元素极性解析：实际 MP 消耗、实际威力倍率与火冰 aspect 判定
│   │   │   │   ├── BlackMageJobStateMachine.cs       黑魔主类：static Tag/注册、Bind 注入、行为分派、通晓/MP 时间资源、GCD/威力/瞬发/时序资源
│   │   │   │   └── BlackMageValidators.cs            黑魔行为合法性校验（reason 契约）
│   │   │   ├── Machinist/                            机工职业子状态机目录
│   │   │   │   ├── MachinistApplications.cs          机工行为应用：连击/资源、过热/整备、野火与替代技能状态
│   │   │   │   ├── MachinistConstants.cs             机工职业常量表：连击阶段、野火命中上限与过热额外威力
│   │   │   │   ├── MachinistJobStateMachine.cs       机工主类：static Tag/注册、Bind 注入、行为分派、连击/过热/野火计时与威力
│   │   │   │   └── MachinistValidators.cs            机工行为合法性校验（reason 契约）
│   │   │   ├── IJobStateMachine.cs                   职业子状态机契约；注册时间资源与可选行为以默认接口实现表达默认行为
│   │   │   ├── JobMachineRegistry.cs                 职业注册中心：按标签注册/查询/枚举，重复标签抛错；内置职业静态构造期反射自动发现注册（扫描程序集 IJobStateMachine 实现，注册中心不引用具体职业，模块自动发现）；自动发现与 Register&lt;T&gt; 共用同一写路径，幂等语义对称
│   │   │   └── JobResourceAccessors.cs               职业共享量谱访问基类：统一 System 绑定、资源读写、最大值读取、时间资源截止时刻读写与 Python int 截断语义，职业类只保留职业规则
│   │   ├── Skills/                                   技能索引层
│   │   │   └── SkillBook.cs                          按 key / game_id 查询技能定义，系统+职业技能合并，重复 key/id 抛错
│   │   ├── Config/                                   配置加载目录
│   │   │   ├── EngineConfigs.cs                      运行期/引擎时序/引擎资源/威力倍率/自然回蓝配置 record
│   │   │   ├── PrecisionConfig.cs                    tensor 精度配置：从 config/precision.yaml 解析 int/float dtype，precision 键缺省走默认值、别名表（fp32/fp16/bf16 等）与错误消息对照 load_precision_config
│   │   │   ├── ProjectConfigLoader.cs                配置加载主入口：职业路由、技能/状态/量谱构建、raid_buff 约束与跨配置冲突校验
│   │   │   ├── SchemaConfigLoader.cs                 共享 schema 加载（config/schema.yaml，C# 与 Python 单一事实来源）：进程内单例（Load 加锁），RequireStateVectorFields 严格字段组访问；构造器缺 projectRoot 时经 RepoRootLocator 按仓库根兜底加载
│   │   │   └── SystemJobConfigs.cs                   系统/职业/项目配置 record（含 JobConfig 计算属性）
│   │   ├── Outputs/                                  输出层
│   │   │   ├── ContextBuilders/                      上下文装配目录
│   │   │   │   ├── CandidateSkillContextBuilder.cs   候选技能上下文：复用同一 SkillTokenBuilder.Build
│   │   │   │   ├── CandidateStateContextBuilder.cs   候选状态上下文：复用同一 StateTokenBuilder.Build，非法候选 before+null
│   │   │   │   ├── SkillHistoryContextBuilder.cs     技能历史上下文：复用同一 SkillTokenBuilder.Build + 条目引用增量缓存
│   │   │   │   └── StateHistoryContextBuilder.cs     状态历史上下文：复用同一 StateTokenBuilder.Build + 条目引用增量缓存
│   │   │   ├── TokenBuilders/                        token 装配目录
│   │   │   ├── BuffVectorTokenBuilder.cs             Buff 向量：系统/职业状态 + buff 分组资源，当前/历史共用同一向量函数（字段模板由 config/schema.yaml 提供并按字段动态装配，未知字段 fail-loud）
│   │   │   │   ├── PlayerVectorTokenBuilder.cs       玩家向量：当前/历史共用同一向量函数（字段列表由 config/schema.yaml 提供并按字段动态装配，未知字段 fail-loud）
│   │   │   │   ├── ResourceVectorTokenBuilder.cs     量谱向量：before/after/consumed 三段
│   │   │   │   ├── SkillTokenBuilder.cs              统一技能 token：单一 Build 函数，技能历史与候选技能共用
│   │   │   │   ├── StateTokenBuilder.cs              统一状态 token：单一 Build 函数，状态历史与合法候选共用；非法候选保留真实 before + null 遮罩
│   │   │   │   └── TargetBuffVectorTokenBuilder.cs   目标 Buff 向量：注册 DoT + 目标威力摘要（字段模板由 config/schema.yaml 提供并按字段动态装配，未知字段 fail-loud）
│   │   │   ├── CandidateContextEntry.cs              候选装配条目：预览 + 消耗量谱 + before/after 状态上下文
│   │   │   ├── GcdStateFormatter.cs                  GCD 制状态快照（秒制量谱转 *_gcd）
│   │   │   ├── ModelTensorFormatter.cs               tensor 输出翻译：统一上下文按精度适配器打包
│   │   │   ├── ModelVectorFormatter.cs               canonical 输出翻译：按统一 schema 裁剪
│   │   │   ├── OutputContextBuilder.cs               输出上下文总装配：顶层七键 + 候选条目装配（状态上下文引用缓存）
│   │   │   ├── OutputContextSchema.cs                canonical 输出 schema：版本/顶层键/状态向量分组/元数据（定义由 config/schema.yaml 提供）
│   │   │   ├── SceneContextSchema.cs                 scene_context 统一 schema：四种窗口 feature keys 与稳定空壳（字段模板由 config/schema.yaml 提供）
│   │   │   ├── SecondsStateFormatter.cs              秒制状态快照
│   │   │   ├── StateContext.cs                       状态上下文原料（job/player/target/resources/buffs/dots）
│   │   │   ├── StateContextBuilder.cs                状态上下文装配（对照 build_state_context）
│   │   │   ├── StateOutputRouter.cs                  输出路由器：秒制/GCD 制快照与 canonical/tensor 调度
│   │   │   ├── TensorPayloadPacker.cs                嵌套载荷递归打包（无 torch：{values,is_null} 结构）
│   │   │   ├── TensorPrecisionAdapter.cs             tensor 输出适配层：按 dtype 量化数值（float32/float16/bfloat16 逐位对齐 torch.tensor 取值，bfloat16 为 double 单次舍入）
│   │   │   └── ValueUtils.cs                         输出层数值转换
│   │   ├── Policy/                                   FightEngine 之上的模型控制动作上下文层
│   │   │   ├── PolicyActionRegistry.cs               policy 控制动作索引及与真实技能 key/id 的冲突校验
│   │   │   ├── PolicyConfigLoader.cs                 独立加载 config/policy_actions.yaml，不进入项目技能配置
│   │   │   ├── PolicyContextBuilder.cs               合并真实技能预演、policy 候选及统一决策历史
│   │   │   ├── PolicyDecisionHistory.cs              记录不提交给游戏状态机的 policy 决策并支持 snapshot/fork
│   │   │   └── PolicySession.cs                      状态机门面的 policy 会话：装配含 policy 候选的完整 vector context，并统一持有决策历史快照
│   │   ├── Properties/                               程序集元数据目录
│   │   │   └── AssemblyInfo.cs                       向测试程序集开放内部规则边界
│   │   └── Facade/                                   门面层
│   │       ├── CandidatePreview.cs                   候选预演结果 record（非法候选不预演，CandidateAfterState 为 null）
│   │       ├── CandidatePreviewBuilder.cs            从完整 timeline snapshot fork 并以 SubmitAction 预演真实技能
│   │       ├── CombatStateMachine.cs                 内部规则/事件分派器：职业路由、校验、动作时序计划、效果与历史记录，不持有推进游标；支持日志实际读条时长覆盖，并区分已排队动作与立即可执行 oGCD
│   │       ├── JobSimulator.cs                       面向宿主的绝对时间模拟器：SubmitAction/AdvanceTo/ObserveAt、外部事实、容量一队列、服务器提前效果与完整读条结束事件、可配置初始绝对时刻及 snapshot/fork；转发实际读条时长并按请求时刻锚定 GCD
│   │       ├── RandomSequenceReplay.cs               固定 seed 随机策略回放：显式选择动作或下一事件绝对时间
│   │       ├── ReplayStateCache.cs                   增量回放缓存：保存完整 SimulationSnapshot 并只暴露绝对时间推进/提交
│   │       └── SequenceRunner.cs                     绝对时间动作请求序列回放；不再根据 reason 猜测推进秒数
│   ├── FightEngine.Tests/                            xUnit 测试工程，按 Common/Config/Facade/Jobs/Outputs/Policy/Skills/System 分组
│   │   ├── GlobalUsings.cs                           测试程序集共享命名空间导入
│   │   ├── SystemTimelineTestDriver.cs               系统规则测试专用的绝对时间线驱动器
│   │   ├── TimelineTestDriver.cs                     职业/输出规则测试专用的绝对时间驱动器
│   │   ├── TestSchemaInit.cs                         测试程序集加载时统一初始化共享 schema（ModuleInitializer，config/schema.yaml）；程序集级禁用并行（单例状态重置测试隔离）
│   │   ├── Common/                                   公共层测试目录
│   │   │   ├── ProjectConfigLoaderTests.cs           配置加载测试：真实仓库解析 + 校验错误路径
│   │   │   ├── SceneWindowTests.cs                   scene 契约常量/索引映射缓存/窗口索引解析/缺失字段抛错（常量来自 config/schema.yaml）
│   │   │   └── YamlConfigTests.cs                    YAML 标量解析边界测试：前导零拒绝/hex/bin/下划线/inf/引号
│   │   ├── Config/                                   共享 schema 测试目录
│   │   │   └── SchemaConfigLoaderTests.cs            未 Load 行为锁定（反射重置单例）、严格字段组访问、团辅窗口模板展开
│   │   ├── Outputs/                                  输出层测试目录
│   │   │   ├── OutputsHistoryTests.cs                历史上下文：MP/量谱前后与消耗、系统/职业 Buff、DoT、威力缩放、秒/GCD 快照
│   │   │   ├── OutputsPrecisionTests.cs              tensor 精度：bfloat16/float16 量化 torch 边界参考、dtype 别名/缺省键/错误消息
│   │   │   ├── OutputsSchemaTests.cs                 canonical/tensor 结构、空 scene、schema 元数据、2.17 GCD 缩放、precision 解析
│   │   │   ├── OutputsTestKit.cs                     输出层测试共享：仓库根定位/状态机构造/向量取值辅助
│   │   │   └── OutputsTokensTests.cs                 候选上下文：拆分、同 schema 复用、非法 null 遮罩、冷却充能快照、单函数 token 构建
│   │   ├── Facade/                                   门面测试目录
│   │   │   ├── CombatStateMachineTests.cs            门面：职业路由/构造校验/决策循环/冷却/可用技能/回放缓存/历史
│   │   │   ├── FacadeKit.cs                          最小内嵌配置与测试职业注册辅助
│   │   │   ├── JobSimulatorTests.cs                  绝对时间门面：提交/队列/推进/观测/外部事实/snapshot/fork 及失败不推进，覆盖请求时刻锚定 GCD 与队列期间立即 oGCD
│   │   │   ├── RandomSequenceReplayTests.cs          固定 seed 绝对时间随机回放的进程内确定性测试
│   │   │   ├── SequenceRunnerTests.cs                序列回放辅助：请求序列/自动补推进原因/只保留最终 canonical 输出/历史完整记录
│   │   │   └── TestJobStateMachine.cs                测试最小职业，供门面组合逻辑复用
│   │   ├── Jobs/                                     职业注册中心与职业状态机测试目录
│   │   │   ├── Black.Mage/                           黑魔职业状态机测试目录
│   │   │   │   └── BlackMageJobStateMachineTests.cs  黑魔规则：AF/UI/火苗/悖论/通晓/雷云/灵极魂/回蓝/人工循环回放
│   │   │   ├── Machinist/                            机工职业状态机测试目录
│   │   │   │   └── MachinistJobStateMachineTests.cs  机工规则：资源/连击/过热/替代技能/野火/起爆/downtime
│   │   │   ├── JobMachineRegistryTests.cs            同类型幂等/冲突抛错/工厂占用/查询与排序/自动发现后手动注册同类型幂等
│   │   │   ├── JobResourceAccessorsTests.cs          公共量谱访问基类：资源读写、最大值、类型转换与未绑定 fail-fast
│   │   │   └── RegistryTestJobs.cs                   注册测试专用职业 A/B（同 tag 不同类型）
│   │   ├── Policy/                                   模型控制动作边界测试目录
│   │   │   └── PolicyContextBuilderTests.cs          wait 不进入状态机、候选保持 25、policy 决策无游戏副作用及冲突校验
│   │   ├── Skills/                                   技能索引测试目录
│   │   │   └── SkillBookTests.cs                     按 key/id 查询/重复抛错/启用过滤/项目合并顺序及技能可选集合默认值
│   │   └── System/                                   系统状态机测试目录
│   │       ├── CombatTimelineRuntimeTests.cs         时间线内核：绝对时间、稳定顺序、mutation、快照/fork
│   │       ├── PlayerStateRuntimeTests.cs            玩家态：时序应用/时间推进/停手切换/六项校验
│   │       ├── PublicTimelineResourceTests.cs        公共资源：分段等价、截止时间、刷新取消、MP/DoT 边界与绝对时间原点
│   │       ├── RegistryTests.cs                      Buff 与量谱注册中心测试
│   │       ├── SystemStateMachineTests.cs            门面：五段编排/回蓝/威力解析/历史
│   │       ├── SystemTestKit.cs                      测试共享配置与技能构造辅助
│   │       ├── TimelineAuthorityTests.cs             时间线 API 权威性守卫：唯一推进内核、稳定优先级、轻量动作协议及阶段 4 旧接口/兼容类型零残留
│   │       └── TimelineTests.cs                      状态/DoT/冷却时间轴测试
│   └── PythonBridge/                                 Python.NET 进程内调用工程；输出 CoreCLR 运行时配置、FightEngine 依赖和观测容器桥接程序集，不启动独立宿主进程
│       ├── PythonBridge.csproj                       net10.0 桥接工程；引用 FightEngine 和项目 .venv 中 pythonnet 包的 Python.Runtime.dll，并生成 runtimeconfig
│       └── NativeContextMarshaller.cs                递归映射 CLR 字典、列表、数组、标量和 null 为 Python 原生对象
├── common/                                           跨顶层模块复用且无具体业务归属的公共层；按职责进入子目录，模块内部共享 helper 必须留在所属模块
│   ├── policy/                                       跨预训练、GRPO、回放和 ONNX 共用的策略模型、输入契约与数据组件
│   │   ├── __init__.py                               策略共享层入口，不隐式导入可选依赖
│   │   ├── config.py                                 策略模型配置与模型/设备/checkpoint/cache 路径解析；按 `.env` 的 `FFXIV_JOB_TAG` + `FFXIV_MODEL_VARIANT` 解析 config.yaml，并合并 model/training/grpo/action_quality 子配置，不依赖 training
│   │   ├── data/                                     策略输入、compiled cache、归一化和候选词表契约
│   │   │   ├── __init__.py                           策略数据契约导出入口
│   │   │   ├── candidate_order.py                    候选 canonical 顺序文件解析与 source→target 行重排工具
│   │   │   ├── compiled_cache.py                     最终 compiled cache 的只读 reader、history bank、签名与统一目录定位；新缓存按副本/区间写入，读取兼容旧平铺缓存
│   │   │   ├── input_contract.py                     checkpoint 输入契约、schema 与归一化规则的序列化/恢复
│   │   │   ├── normalization.py                      归一化配置数据类与 YAML 加载
│   │   │   ├── normalizer.py                         状态、技能、资源、Buff 和 scene 字段的统一归一化器
│   │   │   ├── policy_actions.py                     独立 policy 控制动作配置加载器
│   │   │   ├── schema.py                             raw source、compiled cache 与模型输入共用 schema 定义
│   │   │   ├── skill_vocab.py                        正式技能与 policy 动作组成的模型技能词表
│   │   │   └── spec.py                               从 compiled cache schema 推导模型输入数据规格
│   │   ├── model/                                    与职业无关的策略模型结构、输入编码和推理组件
│   │   │   ├── __init__.py                           策略模型组件导出入口
│   │   │   ├── activation.py                         全模型共用激活算法：选项校验、门控判定、隐层宽度折算与门控合成
│   │   │   ├── attention.py                          split attention 可视化 mask 公共入口
│   │   │   ├── attention_masks.py                    split attention 可见性 mask 与分析矩阵构建
│   │   │   ├── attention_residual.py                 Full Attention Residual 深度聚合器
│   │   │   ├── attention_utils.py                    Q/K/V 投影、头维度整形与 Transformer 层收尾辅助
│   │   │   ├── attention_variants.py                 split attention 变体与单段 attention 执行
│   │   │   ├── candidate_scorer.py                   候选评分模块，按配置激活融合 CLS 与候选 Transformer hidden 生成 logits
│   │   │   ├── grouped_attention.py                  MQA/GQA 投影与按 KV 组 SDPA 适配
│   │   │   ├── input_encoder.py                      skill/state pair embedding（融合层跟随配置激活）、历史 gather 与 token 编码
│   │   │   ├── kv_cache.py                           推理专用 scene/history prefix 分层 KV-Cache
│   │   │   ├── model.py                              通用候选 Transformer 模型编排入口
│   │   │   ├── position_encoding.py                  唯一 RoPE 实现
│   │   │   ├── repetition.py                         连续重复动作软惩罚策略
│   │   │   ├── split_encoder.py                      prefix/candidate/CLS 主编排、Full AttnRes 与 KV-Cache 层路径
│   │   │   └── trace.py                              Transformer hidden、attention head 和 LayerNorm 分析 trace
│   │   └── replay.py                                 回放配置数据类；不包含回放执行实现
│   ├── training/                                     跨训练与 GRPO 共用的训练指标组件
│   │   ├── __init__.py                               共享训练组件入口
│   │   ├── metrics.py                                设备端指标累积与集中 CPU 读取
│   │   ├── tensorboard.py                            BC 与 GRPO 共用的可选 SummaryWriter，按模型变体共享 `<variant>_tensorboard` 事件根目录并写入有限数值标量
│   │   └── tensorboard_server.py                     按 BC/GRPO 配置开关启动监控，使用根目录 `.env` 的模型选择与端口，支持后台就绪检查、训练入口退出时回收服务和打开本机 Web 视图
│   ├── __init__.py                                   公共函数包入口，不隐式导入可选依赖
│   ├── cache_compilation.py                          跨模型分析与回放复用的 raw JSON → compiled cache 转换 CLI 调用入口；编译缓存保存完整 history，模型窗口由读取侧决定
│   ├── config.py                                     配置加载层：把项目 YAML、职业 YAML 与统一 tensor 精度 YAML 装配成 Python 数据结构，并按根目录 `.env` 的 `FFXIV_JOB_TAG` 选择职业；职业 `resources.<key>.max_value` 供训练归一化使用；对外提供 `load_project_config()` / `load_precision_config()`
│   ├── contracts.py                                  跨状态机、输出和训练层共享的 scene 契约常量（scene context key、绝对时间模式、统一滑步窗口、场景浮点容差），定义由 `config/schema.yaml` 提供
│   ├── dataset_layout.py                             跨下载、转换、训练共享的数据阶段目录约定、百分位分档、JSON 发现与路径映射；自动定位 raw/annotated/.cache 根目录，保留副本/区间/文件名
│   ├── gcd_utils.py                                  公共 GCD 单位转换 helper，统一维护秒数与 GCD 数量之间的安全换算
│   ├── models.py                                     转换/训练契约使用的纯数据模型：技能、状态、DoT、冷却和校验结果等数据类
│   ├── numeric.py                                    跨模块复用的嵌套数值映射展开；调用方通过 `ignored_keys` 负责字段过滤
│   ├── output_context_schema.py                      canonical 输出 schema 定义：顶层 keys、状态向量分组与版本由 `config/schema.yaml` 提供
│   ├── project_config.py                             跨模块复用的项目根路径、基于 `python-dotenv` 的 `.env`、职业/模型变体与 TensorBoard 端口解析、配置驱动职业列表 `resolve_registered_job_tags()`
│   ├── scene_context_schema.py                       `scene_context` 统一 schema 定义：窗口字段模板与常量由 `config/schema.yaml` 提供
│   ├── scene_window.py                               scene window feature key 索引映射缓存与窗口索引解析
│   ├── schema_config.py                              共享 schema 配置加载（`config/schema.yaml`，C# 与 Python 单一事实来源），进程内一次性缓存
│   ├── skills.py                                     技能索引层：按 `key` / `game_id` 提供统一查询入口
│   ├── torch_dependencies.py                         跨模块复用的 PyTorch 延迟导入与缺失依赖错误
│   ├── torch_runtime.py                              跨训练、回放和模型分析复用的 batch 搬运、模型 dtype 与 autocast 工具
│   ├── torch_serialization.py                        跨训练、回放、模型分析和 compiled cache 复用的 `weights_only=True` 安全 PyTorch 文件读取入口，并集中维护必要的安全类型允许列表
│   └── yaml_config.py                                跨状态机、转换和训练模块复用的 YAML mapping 加载与顶层类型校验；仅空文档/`null` 归一化为空 mapping，其他非 mapping 值保留并报错
├── config/                                           模拟器 YAML 配置目录，存放项目级、系统级和职业级静态定义
│   ├── convert_fflogs/                               FFLogs 转换专用配置目录，集中维护转换脚本默认职业与按职业拆分的日志解释规则
│   │   ├── jobs/                                     FFLogs 转换职业配置目录，每个职业一个 YAML，只写该职业的日志解释与 GCD 探针规则
│   │   │   ├── black_mage.yaml                       黑魔 FFLogs 转换配置，定义基础 GCD 探针技能、`probe_skill_cast_time_seconds` 与需排除的加速 Buff ID
│   │   │   └── machinist.yaml                        机工 FFLogs 转换配置，定义机工基础 GCD 探针技能与职业专用日志解释参数
│   │   └── default.yaml                              FFLogs 转换全局默认配置，只维护 worker 与 GCD 检测公共默认值；职业标签统一读取根目录 `.env` 的 `FFXIV_JOB_TAG`
│   ├── skills/                                       职业配置目录，每个职业一个 YAML，只写该职业自己的静态定义
│   │   ├── black_mage.yaml                           黑魔职业配置，定义黑魔 timing、resources 量谱上限、statuses、skills、默认 `value=1.0` 和技能级 AoE 额外目标降低比例
│   │   └── machinist.yaml                            机工职业配置，定义机工资源、状态、技能决策 `value`、野火延迟威力和冷却/连击规则所需的静态参数
│   ├── models/                                       训练与推理共用的策略模型配置，按 `.env` 的 `FFXIV_JOB_TAG` 与 `FFXIV_MODEL_VARIANT` 选择职业目录下的模型变体
│   │   └── <job_tag>/<variant>/                      模型变体配置目录；`action_quality.yaml` 定义动作质量负监督的轻微/中等/严重等级权重，由 `config.yaml` 的 `action_quality_config` 引用并合并（尚未接入训练损失）；`config.yaml` 是清单，`model.yaml` 提供模型架构、raw/output 与统一 precision，`training.yaml` 提供预训练/DataLoader/增强/序列过采样，`grpo.yaml` 提供 GRPO rollout 与更新参数（当前 Artzip 每场 16 条样本、仅按 1200 秒时间上限终止），候选顺序文件与模型变体放在同一目录
│   ├── policy/                                       跨训练与策略运行时共享的策略输入配置
│   │   └── normalizer.yaml                           compiled cache 与训练输入共用的归一化配置
│   ├── action_quality.yaml                           离线动作质量桥接运行配置，维护外部分析源码路径与单份分析超时；等级权重属于职业模型配置
│   ├── default.yaml                                  项目总配置入口，只注册 `runtime.job_configs` 职业配置映射和系统配置路径；当前职业严格由根目录 `.env` 的 `FFXIV_JOB_TAG` 选择，不再声明 `active_job` 或单一 `job_config`
│   ├── policy_actions.yaml                           独立 policy 控制动作配置；`ogcd_wait` 保留 raw id 0，但不进入 FightEngine SkillBook
│   ├── precision.yaml                                tensor 精度配置入口，统一定义状态机 tensor 输出与最终 compiled cache 的整数/浮点 dtype
│   ├── schema.yaml                                   共享 schema 定义（C# 与 Python 单一事实来源）：共享常量（滑步窗口/绝对时间模式/scene context keys）、scene context 窗口字段模板与 segment kinds、canonical 输出顶层 keys 与状态向量分组、状态向量字段模板；动态部分（职业量谱/Buff/DoT 列表）仍由运行时推导
│   └── system.yaml                                   系统级共享配置，定义基础 GCD、技能表基准、战斗内自然回蓝 tick、目标威力倍率、爆发药及统一团辅窗口（含 `raid_buff_window.marker_skills` 与权威的 `duration_seconds`）
├── data/                                             原始训练数据目录，大文件不进版本控制
│   └── human/                                        人工战斗记录根目录
│       └── job/                                      按职业目录名存放训练数据
│           └── {job}/                                具体职业训练数据目录，转换输出与训练缓存按职业隔离
│               ├── .cache/                           最终 compiled cache，按 <副本>/<区间>/ 保存 manifest 与分片；独立或旧未分类输入保持对应层级，读取兼容旧平铺缓存
│               ├── annotated/                        原始 raw 与离线分析结果合并的评估 JSON，复用公共目录映射保留 <副本>/<区间>/，不替换原始数据
│               ├── grpo/                             GRPO 自回归 rollout 持久化目录，按 run/iteration 保存 manifest 与轨迹 tensor，不纳入版本控制
│               └── raw/                              FFLogs 原始 JSON 输入，只读，不接收转换输出
│                   └── .../                          各个副本原始日志目录
├── docs/                                             项目文档目录，记录问题清单、迁移分析和训练上下文规划
│   ├── fflogs-timestamp-semantics-2026-09-14.md      FFLogs raw JSON 时间戳真实语义实测：begincast=读条开始+实际读条时长、cast=服务器提前结算、damage=落地按技能固定偏移、DoT 3s 节拍与 prepull 裁剪无负时间戳；转换层正常硬读条直接取 begincast，瞬发取 cast，仅开怪首个预读按配置 0.5s 滑步窗口恢复请求，随后统一归零
│   ├── GRPO_算法与实现.md                             GRPO 算法详解与实现指南，公式与超参照抄三篇论文原文
│   ├── 命令行使用说明.md                              各命令入口与完整参数说明：统一入口、FFLogs 下载、训练、GRPO、ONNX 导出、模型分析和自回归回放
│   ├── 项目各文件说明.md                              项目结构说明
│   └── timeline-driven-state-machine-refactor-plan.md 时间线驱动状态机重构权威计划，含阶段进度、实施边界与完成标准
├── grpo/                                             独立 GRPO 强化学习后训练包，与预训练训练循环和模型配置实现分离
│   ├── __init__.py                                   GRPO 配置导出入口
│   ├── __main__.py                                   `python -m grpo` 模块入口
│   ├── config.py                                     GRPO 参数定义、校验与 YAML 配置读取
│   ├── grpo.py                                       GRPO 后训练命令行编排，读取 checkpoint、场景和 GRPO 专属配置
│   ├── storage.py                                    GRPO rollout 的原子磁盘写入、manifest 与按 minibatch 流式读取
│   └── trainer.py                                    状态机 rollout、16 条组内采样与奖励标注、组内相对优势、裁剪策略损失、KL、贪心保护、TensorBoard 更新/迭代指标和 checkpoint 保存；采样/标注阶段 inference-only，轨迹完成后才反向更新，GRPO 自回归只受 `max_duration_seconds` 时间上限约束，轨迹 tensor 由 `storage.py` 持久化而非整轮留在内存
├── old/                                              旧实现归档区，保留历史状态机、训练代码、数据和文档，不作为新代码入口
│   ├── data/                                         旧训练数据、战斗记录和回放样本
│   ├── docs/                                         旧文档目录，包含人工循环等参考资料
│   ├── State_Machine/                                旧状态机实现与相关资源
│   ├── Tools/                                        旧工具脚本和辅助程序
│   └── Training/                                     旧训练代码与配置
├── preview/                                          README 和项目展示使用的预览资源目录，不承载业务代码
├── scripts/                                          辅助脚本目录，负责 FFLogs 抓取、原始日志转换、训练样本导出、模型回放分析和独立 ONNX 导出
│   ├── action_quality/                               离线动作质量桥接，自动选择职业/副本模块，原始 JSON 加 analysis 后保存到 annotated 对应层级
│   │   ├── runtime/                                  Node 无界面分析运行层，不修改上游代码
│   │   │   ├── extract/                              通用与职业专属结构化证据提取器
│   │   │   │   ├── black_mage.cjs                     黑魔循环、Proc、DoT 和资源证据；展示优先级不当作严重度
│   │   │   │   ├── common.cjs                         通用读条中断、插入和连击问题定位
│   │   │   │   └── machinist.cjs                      机工野火/超荷/整备/机器人窗口、冷却偏移和量谱历史
│   │   │   ├── analyze.cjs                           调用上游适配器与 Parser，选择模块并构造统一分析契约
│   │   │   ├── bootstrap.cjs                         复用上游 Babel 变换，补齐 DOM 与样式加载环境
│   │   │   ├── package.json                          独立 DOM 依赖声明
│   │   │   ├── package-lock.json                     桥接 DOM 依赖锁文件
│   │   │   ├── references.cjs                        原始事件精确索引匹配，保留 actor 实例、重复和未匹配语义
│   │   │   ├── routing.cjs                           根据日志职业和副本选择模块，未知职业拒绝降级
│   │   │   ├── run.cjs                               独立 Node 子进程入口，只输出临时分析 JSON
│   │   │   └── serialize.cjs                         消息、Map 和证据数据序列化，不执行网页组件
│   │   ├── __init__.py                               包入口
│   │   ├── __main__.py                               python -m scripts.action_quality 入口
│   │   ├── cli.py                                    批量输入发现、进度、失败汇总与退出状态
│   │   ├── config.py                                 复用 .env、策略模型 YAML 和职业严重度权重
│   │   └── runner.py                                 调用 Node、校验源文件身份并原子保存合并 JSON
│   ├── autoregressive_replay/                        状态机驱动的模型自回归回放包，通过共用 `PolicyBackend` 在 PyTorch checkpoint 与已验证 ONNX 部署包之间切换；C# 状态机由进程内 `InProcessBackend` 直接调用，模型后端共用 live batch、重复惩罚、合法 mask、采样和轨迹推进，并支持逐决策 PT/ORT parity、性能/PPG 报告及固定完整轨迹上的历史长度消融；`AutoregressiveReplaySession` 长驻复用状态机/cache，按轨迹 reset，`ReplayCacheStore` 负责 reader 与 shard cache 生命周期
│   │   ├── __init__.py                               回放包导出入口
│   │   ├── __main__.py                               `python -m scripts.autoregressive_replay` 入口
│   │   ├── backends.py                               PyTorch/ORT 共用 policy backend 契约；ORT 在创建 session 前严格校验部署包和已有发布证明，集中执行固定容量补位、raw logits 与有序 Top-3 parity、宿主策略选择一致性及延迟/峰值内存统计
│   │   ├── config.py                                 回放配置加载，按 `.env` / CLI 选择 PyTorch checkpoint 或 ONNX 部署包；ORT 从 manifest 自动恢复职业和正式 history 容量，同时读取 scene、解码和时间轴参数；PyTorch 可显式覆盖分析精度，ORT 精度始终以 manifest 为准
│   │   ├── context.py                                使用 C# 状态机后端实时历史/候选上下文，按绝对时间 cache scene 与空 scene 提交类型化外部事实；通过 `apply_external_event` 同步 Boss 可选中、移动、目标数与团辅窗口，移动窗口在结束前 0.5 秒进入滑步豁免；通过 `observe_at` 构建 live batch，不再读取可变 Python state 镜像；`build_from_canonical` 支持历史消融旁路重算
│   │   ├── main.py                                   回放 CLI 编排入口，支持 backend/ORT EP、PyTorch 显式分析精度、temperature/top-p、历史消融，以及 `--parity-onnx-package` 双后端完整轨迹验收
│   │   ├── outputs/                                  回放结果输出模块目录
│   │   │   ├── __init__.py                           回放输出导出入口
│   │   │   └── markdown.py                           输出 backend、实际 Execution Provider、模型来源、PPG、性能指标及逐步动作/合法 Top-K，宿主控制动作 `ogcd_wait` 简写为 `-`；历史消融结果额外输出完整轨迹动作对照列
│   │   ├── parity.py                                 在同一 PyTorch 参考轨迹逐决策双跑 PT/ORT；拒绝非有限或负数容差，中途不因差异短路，成功或失败都输出绑定四类产物 SHA、目标运行库、raw logits、双方有序 Top-1/Top-3、宿主选择、首个分叉、性能、动作序列和 PPG 的完整 JSON；仅显式 workflow 门禁可登记部署包，普通 CLI 始终只写调试报告
│   │   ├── ppg.py                                    训练期间的 PPG 评估：`val_ppg` 从验证 source 开始绝对时间自回放，`none_ppg` 从空 scene/满 MP 状态自回放，两者共用 `DecisionScheduler` 与进程内 C# 状态机
│   │   └── replay.py                                 依赖 `PolicyBackend` 与进程内状态机接口执行 C# 自回放；把真实技能通过 `submit_action` 提交、`ogcd_wait` 通过 `record_policy_action` 记录，时间推进统一委托 `DecisionScheduler`，观测使用 `observe_at`；保留温度/top-p、KV cache、轨迹快照与历史消融
│   ├── common/                                       脚本包公共辅助目录：承载跨脚本复用的进程内 C# 状态机适配器、结果类型与场景事实/输出改写层
│   │   ├── __init__.py                               公共辅助包入口，不隐式导入可选依赖
│   │   ├── inprocess_backend.py                      Python.NET 进程内状态机适配器；校验实际 FightEngine 契约版本，并通过 PythonBridge 将观测直接映射成 Python 原生容器
│   │   ├── json_io.py                                下载与离线标注共用的 UTF-8/LF 原子 JSON 写入，失败保留已有文件
│   │   ├── scene_state.py                            场景事实调度与输出层场景标量合成：转换链路只按时间提交 Boss 可选中/目标数/团辅窗口，移动由输出层合成并应用滑步豁免；在线回放由 SceneTemplateProvider 另行提交经豁免的 movement_changed，目标数按分段常量时间线在区间端点显式归位到单目标，并提供按绝对时刻查询的移动/停手标量与 canonical player 向量改写
│   │   └── state_machine_types.py                    进程内 C# 状态机 API 的不可变返回类型：时间线位置、动作提交/校验、外部事实、policy 决策和观测结果
│   ├── convert_fflogs/                               FFLogs 原始 JSON 转换包，负责提取动作、构建场景上下文、驱动状态机回放并直接编译最终训练 cache
│   │   ├── cache/                                    compiled cache 子包：raw 路径选择、编译编排、加载与 shard 写入
│   │   │   ├── __init__.py                           compiled cache 门面，转发 raw 路径选择、缓存编译和缓存加载入口，不承载具体实现
│   │   │   ├── cache_compile.py                      raw JSON → compiled cache 的编译编排，负责按副本补位、并行 worker、失败记录、dtype 工具和最终 cache 写入；编译阶段固定保留完整状态机历史与增量 history bank，`max_history` 不参与 cache 身份；每个 source 先构建一次 bank，再让样本只保存窗口引用
│   │   │   ├── cache_load.py                         raw JSON 对应 compiled cache 的签名校验与加载，只读取最终 cache，不触发 raw 转换；支持由 replay session 注入共享 shard cache，避免同一 GRPO 运行重复展开分片
│   │   │   ├── cache_paths.py                        复用数据集目录层发现 raw JSON，按副本比例选择首轮文件和失败补位候选分组
│   │   │   └── cache_writer.py                       最终 compiled cache manifest/shard 写入器，只写 `.cache`，不写 raw 或中间训练格式；manifest 持有 source 级 history bank 与稳定 `history_bank_id`，shard 样本只写 `history_end` / `history_length`
│   │   ├── config/                                   转换配置子包：YAML 与根目录 `.env` 加载、GCD 参数校验和共享常量
│   │   │   ├── __init__.py                           转换配置门面，导出 `config/convert_fflogs/` 与根目录 `.env` 加载入口
│   │   │   ├── config.py                             FFLogs 转换配置加载模块，负责读取 `config/convert_fflogs/`、校验探针读条时长与其他 GCD 参数、解析根目录 `.env` 的 `FFXIV_JOB_TAG` 和职业路由
│   │   │   └── constants.py                          共享常量与时间精度工具
│   │   ├── extraction/                               日志提取子包：动作与请求时刻提取、停手窗口检测和战斗载荷装帧
│   │   │   ├── __init__.py                           日志提取与战斗载荷装帧门面
│   │   │   ├── downtime.py                           Boss 停手窗口检测
│   │   │   ├── extraction.py                         FFLogs 事件提取、请求时刻解析、初始移动标注、强制移动窗口与 GCD 检测；硬读条请求取配对的 `begincast`，读条时长优先使用 `begincast.duration` 并结合技能表基础 GCD 校准，样本不足时回退间隔检测；无 begincast 的瞬发取 `cast`，首个硬读条按 schema 滑步窗口处理，读条来源与顺序校正量均保留供审计
│   │   │   └── fight_payload.py                      战斗载荷装配：以最早请求为 0 同步平移动作与场景事实、保留 request/cast/effect 审计字段，并把 AoE 命中事件交给 scene builder 推导双目标辅助标记；不承载状态机目标倍率逻辑
│   │   ├── scene/                                    scene window 子包：窗口构造、输入规范化与绝对时间查询
│   │   │   ├── __init__.py                           scene window 子包入口，重导出 scene build/query 的正式入口
│   │   │   ├── scene_builders.py                     scene window 构造、输入规范化和 token 生成；从实际双目标命中事件生成仅含 `target_count >= 2` 的辅助标记，按 2s 观测超时切段并在最后事件后保留 1.5s，不生成单目标/停机填充 token
│   │   │   ├── scene_context.py                      scene context 公共导出 facade，汇总 builders 与 queries 的正式入口，包含多目标辅助标记 builder/token
│   │   │   └── scene_queries.py                      绝对时间 scene window 查询：完整 scene_context 视图、人工排查 anchor 与停手总时长；移动与停手标量由 `scripts/common/scene_state.py` 承担
│   │   ├── source/                                   raw source 子包：raw JSON 只读解析与内存训练源读取
│   │   │   ├── __init__.py                           raw source 门面，导出 raw JSON 转换与内存训练源读取器
│   │   │   ├── raw_source.py                         raw JSON 只读解析入口，创建/关闭不限制历史窗口的 C# 后端并把单文件转换结果保留在内存供 cache 编译
│   │   │   ├── source_helpers.py                     raw source 的技能特征提取、nullable 值读取和列式辅助
│   │   │   └── source_reader.py                      读取 raw 转换产生的内存 training source，生成 schema、模型样本输入原料与相邻样本间真实效果历史增量；scene token 从整场 `fight_scene_context` 读取并保持归一化绝对时间
│   │   ├── training/                                 训练样本子包：决策回放、样本编码与历史 bank 构建
│   │   │   ├── __init__.py                           训练样本门面，导出决策回放、样本编码与历史 bank 入口
│   │   │   ├── history_bank.py                       按状态机实际单调增长的效果历史一次性编码 source 级技能/状态 bank，并生成 end-exclusive 窗口引用；允许多个决策共享前缀或一次新增多条效果，不为每个样本重建历史 tensor
│   │   │   ├── sample_builder.py                     把内存 training source 编码为最终 compiled cache 样本；历史部分只写增量 bank 的 end/length 引用，候选/scene 仍按样本编码；只属于脚本转换层
│   │   │   └── training.py                           训练样本回放，通过后端驱动决策循环：严格按归一化日志请求时刻提交动作并转发实际读条时长、提交前发场景事实、`observe_at` 取上下文并按场景改写 player 向量，不按 reason 补推进或重试；`ogcd_wait` 作为 policy 决策记录而不提交状态机，label leak 以真实效果历史前缀对齐断言
│   │   ├── __init__.py                               包导出层，暴露 raw JSON 转换和最终 compiled cache 入口
│   │   ├── cli.py                                    CLI 入口，支持显式输入或自动扫描职业 `raw/` 并写入职业 `.cache`；训练回放口径固定为 FFLogs `cast=动作生效时刻`
│   │   ├── pipeline.py                               报告→训练两级转换流水线；报告阶段从日志解析请求/读条，结合技能表与职业配置检测基础 GCD，再把整场时间线归一到最早请求为 0，训练阶段以 0 初始化后端并导出训练样本
│   │   └── utils.py                                  C# 进程内后端构造（build_backend）、时间戳窗口合并与路径整理
│   ├── fflogs_scraper/                               FFLogs 下载工具包，按 API、配置、格式契约、下载编排和文件 I/O 分层；保存整场报告和事件供离线分析与训练转换
│   │   ├── api/                                      FFLogs V2 API 访问
│   │   │   ├── __init__.py                           API 子包入口
│   │   │   └── client.py                             OAuth、GraphQL 查询、事件分页、玩家解析、API 默认/指定分区与角色历史排名查询
│   │   ├── config/                                   下载常量、环境变量与输入校验
│   │   │   ├── __init__.py                           配置子包入口
│   │   │   ├── constants.py                          请求指纹、事件上限、格式版本与补丁日期常量
│   │   │   ├── environment.py                        通过 python-dotenv 加载根目录 .env，环境变量优先
│   │   │   └── validation.py                         GraphQL 标量、排行指标和报告编号校验
│   │   ├── contracts/                                报告、战斗和事件格式契约
│   │   │   ├── __init__.py                           契约子包入口
│   │   │   ├── events.py                             嵌套技能适配、友好关系与事件完整性标记
│   │   │   ├── models.py                             FightInfo 和 ReportMeta 元数据容器
│   │   │   ├── rankings.py                           历史击杀记录、匿名筛选与 ranking 元数据契约；百分位分档复用脚本公共目录约定
│   │   │   └── report.py                             V2 报告适配、真实 fight ID 选择和训练索引包装
│   │   ├── download/                                 下载与目录查询编排
│   │   │   ├── __init__.py                           下载子包入口
│   │   │   ├── batch.py                              历史百分位十档配额下载、同档失败补位及已有文件校验，输出到副本/区间目录
│   │   │   ├── encounters.py                         副本区域与战斗目录展示
│   │   │   ├── sampling.py                           总量均分、API 默认/指定分区角色发现、匿名排除与历史记录去重；不负责文件下载
│   │   │   └── single.py                             单报告下载，支持整场事件与伤害表模式
│   │   ├── io/                                       下载链接和本地 JSON I/O
│   │   │   ├── __init__.py                           I/O 子包入口
│   │   │   ├── filenames.py                          单场与批量文件名构造及路径字符隔离
│   │   │   ├── json_io.py                            下载 JSON 写入兼容入口，转发 scripts/common/json_io.py
│   │   │   └── urls.py                               FFLogs URL 的 report、fight 和 source 解析
│   │   ├── __init__.py                               公开导出 API 客户端、元数据容器和 URL 解析器
│   │   ├── __main__.py                               python -m scripts.fflogs_scraper 模块入口
│   │   └── cli.py                                    命令参数、认证、中断处理和子命令分发
│   ├── model_analysis/                               科研模型分析 PNG 输出包，中心入口统一加载 checkpoint/raw JSON 对应的 compiled cache 与 YAML 模型精度；cache 缺失或过期时调用转换 CLI 补齐，各 outputs 子模块独立生成分析图
│   │   ├── __init__.py                               分析包入口
│   │   ├── __main__.py                               `python -m scripts.model_analysis` 入口
│   │   ├── common.py                                 checkpoint/cache 加载、统一 YAML precision 的模型 dtype/autocast、loss 轻量上下文、读取职业 YAML 的 candidate_order_file、缺失 cache 时调用转换 CLI、逐批释放 trace 的 hidden 提取、DataSpec 对齐、低内存 CPU PCA、role 颜色约定和统一图表布局
│   │   ├── job_labels/                               按职业路由科研分析标签；未知职业返回 `unknown`
│   │   │   ├── __init__.py                           分析标签职业路由入口
│   │   │   └── black_mage.py                         黑魔 AF/UI/MP 决策状态标签提取
│   │   ├── main.py                                   分析中心编排入口；同一命令先输出基础图，释放重上下文后以相同 checkpoint/数据/精度追加 loss landscape
│   │   ├── outputs/                                  分析图输出模块目录
│   │   │   ├── __init__.py                           输出模块导出入口
│   │   │   ├── attention.py                          基于真实 cache 的候选动作注意力热图、逐层完整 query-key 矩阵、最后一层逐 head 矩阵和 scene/history/candidate/CLS 角色块汇总；mask 区域灰显，矩阵热图使用线性色阶与逐 query 行最大有效权重归一化，角色块汇总保留原始权重语义
│   │   │   ├── hidden_statistics.py                  hidden 分布和按 token role 的范数统计图
│   │   │   ├── loss_landscape.py                     逐 Transformer 层 filter-normalized 正交参数方向的高精度交叉熵曲面/等高线、float64 数值网格与方法元数据导出
│   │   │   ├── pair_embedding.py                     基于真实候选状态的技能-状态 pair fusion embedding PCA 图
│   │   │   ├── pca_layers.py                         每层 hidden 的 2D/3D PCA 图
│   │   │   └── skill_embedding.py                    原始技能 ID embedding PCA 图
│   │   └── token_metadata.py                         按编码器实际 token 位置构造 step、候选、合法性、AF/UI、MP 和 logit 元数据
│   ├── onnx_export/                                  checkpoint 后处理 ONNX 导出脚本包，与训练循环解耦；纯 Tensor policy 复用已有模型权重并输出策略后处理前 logits
│   │   ├── io/                                       ONNX 部署产物公共 I/O
│   │   │   ├── __init__.py                           产物处理子包入口
│   │   │   └── artifact_io.py                         文件/确定性 JSON SHA-256、原子 JSON、确定性 golden NPZ 和 PyTorch 导出报告归一化
│   │   ├── config/                                   ONNX 导出与 parity 的根目录 `.env` 配置
│   │   │   ├── __init__.py                           配置子包入口
│   │   │   └── config.py                             checkpoint、部署包、Provider、scene 和导出门禁参数解析
│   │   ├── contracts/                                固定容量、部署契约和职业 profile
│   │   │   ├── __init__.py                           契约子包入口
│   │   │   ├── contract.py                           固定 batch/scene/history/candidate 容量、padding 和 golden 输入契约
│   │   │   ├── deployment_contract.py                从 checkpoint、模型结构和职业 profile 派生完整部署契约并校验 manifest
│   │   │   └── deployment_profile.py                 读取固化的职业 vocab 与 scene 容量语料统计证据
│   │   ├── export/                                   ONNX 导出执行层，文件按阶段平铺
│   │   │   ├── __init__.py                           导出执行层公开入口，暴露导出 API 与 checkpoint 加载器
│   │   │   ├── api.py                                导出公开 API、临时目录生命周期和失败清理
│   │   │   ├── builder.py                            只负责串联环境、契约、图、运行时和文档阶段
│   │   │   ├── checkpoint.py                         checkpoint 加载、部署 profile 读取和部署契约装配
│   │   │   ├── context.py                            各导出阶段之间传递的契约、ONNX 产物和运行时验收结果
│   │   │   ├── documents.py                          golden、容量报告、导出报告和 manifest 落盘与自校验
│   │   │   ├── environment.py                        ONNX 依赖、精度、设备和 ORT provider 门禁
│   │   │   ├── graph.py                              ONNX 导出、metadata 盖章、checker/shape inference 和体量审计
│   │   │   ├── publish.py                            部署目录原子发布与 Windows 文件占用重试
│   │   │   └── runtime.py                            ORT 会话、运行时契约和 PyTorch/ORT 精度矩阵验收
│   │   ├── policy/                                   纯 Tensor ONNX policy adapter
│   │   │   ├── __init__.py                           policy 子包入口
│   │   │   └── policy.py                             复用 attention/FFN/scorer 权重并输出策略 logits
│   │   ├── profiles/                                 首次语料分析后固化的职业部署画像目录
│   │   │   └── black_mage.json                       黑魔部署画像、技能 vocab 映射与 scene 容量统计证据
│   │   ├── release/                                  ONNX parity 证据与发布状态门禁
│   │   │   ├── __init__.py                           发布子包入口
│   │   │   ├── policy.py                             正式发布场景与动作预算策略
│   │   │   └── release.py                            parity 证据绑定、发布状态晋升和审计校验
│   │   ├── runtime/                                  ORT provider、精度、Tensor I/O 和矩阵验收
│   │   │   ├── __init__.py                           运行时子包入口
│   │   │   ├── ort_runtime.py                        ORT provider 与 session 策略
│   │   │   ├── precision.py                          PyTorch、ORT、TensorProto、manifest dtype 和数值门槛规格
│   │   │   ├── runtime_targets.py                    正式导出与运行库目标矩阵
│   │   │   ├── tensor_runtime.py                     PyTorch/ORT 共用的 Tensor 执行与 BF16 golden 编码
│   │   │   └── validation.py                         动态长度与固定容量的 CPU/CUDA/ORT padding 矩阵验收
│   │   ├── __init__.py                               ONNX 导出脚本包入口
│   │   ├── __main__.py                               `python -m scripts.onnx_export` 无参数导出入口
│   │   ├── manifest.schema.json                      ONNX 部署 manifest 的 JSON Schema
│   │   └── workflow.py                               根目录 PowerShell 菜单的 Python 工作流入口
│   ├── __init__.py                                   脚本目录包入口，便于测试和其他模块复用转换辅助函数
│   └── ffxiv_ccg_menu.psm1                           PowerShell 公共菜单模块：统一菜单编号、命令行 action 名称与交互分发
├── tests/                                            测试目录，按源代码模块拆分，并包含根目录公共工具回归测试
│   ├── grpo/                                         独立 GRPO 后训练测试
│   │   └── test_grpo.py                              GRPO 配置、优势/loss、变长 batch、CLI 和优化器更新测试
│   ├── scripts/                                      脚本模块测试子目录
│   │   ├── __init__.py                               子包入口
│   │   ├── conftest.py                               scripts 测试公共 fixture：cs_backend（托管状态机运行文件未构建时跳过）与 cs_skill_book，转换与自回归回放测试共用
│   │   ├── autoregressive_replay/                    自回归回放测试子目录
│   │   │   ├── __init__.py                           子包入口
│   │   │   ├── conftest.py                           回放测试共享 fixture：dotenv 隔离与最小 checkpoint/scene 文件构造
│   │   │   ├── test_config.py                        回放配置加载测试，覆盖 dotenv 装配、env 覆盖、ONNX manifest 路由和非法模式/上限拒绝
│   │   │   ├── test_context.py                       scene 上下文测试，覆盖 scene 模板归一化/目标数解析、targetable/移动/团辅同步和 live batch 构造
│   │   │   ├── test_parity.py                        PT/ORT parity 测试，覆盖 backend 对比失败报告、固定容量 batch device、完整失败轨迹审计、有限容差和 parity CLI
│   │   │   ├── test_ppg.py                           自回归 PPG 的 GCD 计数、直接/DoT 威力累计和候选顺序校验测试
│   │   │   ├── test_replay.py                        回放核心逻辑测试，覆盖预测模式、解码、KV-cache 开关、历史消融、事件时间线、无合法动作推进、共享 session/cache 生命周期和 Markdown/CLI 输出
│   │   │   └── test_scheduler.py                     共享 `DecisionScheduler` 的绝对时间推进、scene 边界同步、读条/GCD/oGCD 调度与无法动作时间调度测试
│   │   ├── convert_fflogs/                           FFLogs 转换测试子目录
│   │   │   ├── __init__.py                           子包入口
│   │   │   ├── test_cache.py                         raw JSON → compiled cache 测试，覆盖 raw 只读、cache 写入、worker 和训练读取
│   │   │   ├── test_config.py                        FFLogs 转换配置测试，覆盖统一 `FFXIV_JOB_TAG`、职业级探针技能和显式参数优先级
│   │   │   ├── test_convert.py                       FFLogs 转换脚本测试，覆盖动作提取、begincast.duration 基础 GCD 校准、整场战斗保留、多目标辅助标记、场景上下文装配和训练样本构建链路
│   │   │   ├── test_cs_backend.py                    InProcessBackend 动作校验、读条覆盖、无子进程调用和 Python 原生观测类型测试
│   │   │   ├── test_timestamp_normalization.py       FFLogs 请求时间恢复与全局归零测试，覆盖硬读条 0.5s 提前结算、瞬发原时刻、场景同步平移、取消读条配对拒绝和小范围动作顺序稳定化
│   │   │   ├── test_training_builder_basic.py        raw 转换训练样本基础时序与回放测试，覆盖 GCD 覆盖、首拍预读、ogcd_wait 注入、施法时间戳和冷却回放
│   │   │   ├── test_training_builder_label.py        raw 转换训练样本 label 测试，覆盖动作前上下文、直接合法性校验和 label leak 断言
│   │   │   └── test_training_builder_scene.py        raw 转换训练样本 scene 测试，覆盖绝对时间场景上下文、运行时标记和停手边界
│   │   ├── fflogs_scraper/                           FFLogs 下载工具包的职责测试
│   │   │   ├── __init__.py                           测试子包入口
│   │   │   ├── conftest.py                           共用报告、玩家、宠物和敌人实例元数据
│   │   │   ├── test_api.py                           GraphQL 参数校验、事件完整分页与历史排名查询口径测试
│   │   │   ├── test_cli.py                           模块入口、命令分发与根目录 .env 定位测试
│   │   │   ├── test_contracts.py                     报告与事件适配、玩家归属和训练字段兼容测试
│   │   │   ├── test_download.py                      下载模式、分档保存、同档补位、已有文件完整性与提前停止测试
│   │   │   ├── test_io.py                            URL、文件名安全和 UTF-8/LF 原子写入测试
│   │   │   └── test_sampling.py                      十档配额、百分位边界、非最佳历史记录、匿名/隐藏排除、记录去重和 API 分区一致性测试
│   │   ├── onnx_export/                              独立 ONNX 导出脚本测试子目录
│   │   │   ├── __init__.py                           子包入口
│   │   │   ├── test_config.py                        ONNX `.env` 合并、checkpoint→部署目录映射、无重复 history 容量和 parity 参数校验
│   │   │   ├── test_exporter.py                      固定容量/全 dtype 零补位、完整 BF16 版本矩阵、公共确定性 golden I/O、TensorProto dtype 常量、精度专属 parity 容差、产物 SHA 绑定、发布状态晋升、失败不覆盖、profile 路径、ORT provider、报告兼容，以及安装 ONNX extras 后本地执行的 tiny checkpoint 导出/checker/PT-ORT parity 快速门禁
│   │   │   ├── test_policy.py                        纯 Tensor policy 测试，覆盖 scene 类型投影、稳定 masked softmax、逐层 padding 矩阵、raw logits 对齐、策略边界和 `torch.export` 图捕获
│   │   │   ├── test_real_checkpoint_padding.py       本地真实 46.9M 参数 BF16 checkpoint 的 profile/vocab/容量与 CUDA padding 回归；`RUN_REAL_ONNX_EXPORT=1` 时额外执行 BF16 export/checker/ORT 全链路慢速门禁，artifact 缺失时跳过
│   │   │   └── test_tensor_runtime.py                ORT Tensor I/O 职责级测试，覆盖 CPU I/O Binding、bool NumPy 回退、非 bool DLPack 路径和 CUDA BF16 golden NPZ 字节确定性
│   │   ├── action_quality_runtime.test.cjs            Node 职业路由、实例事件匹配、消息序列化与机工量谱循环引用边界测试
│   │   ├── test_action_quality.py                    离线标注 raw 保留、目录映射、失败保护、.env 和职业权重隔离测试
│   │   ├── test_dataset_layout.py                    数据阶段路径映射、区间目录校验、旧目录兼容与转换按副本分组测试
│   │   ├── test_model_analysis.py                    科研模型分析工具的 token 元数据、职业标签、候选人工顺序/DataSpec 对齐、PCA、hidden/attention/pair/skill PNG 输出和 CLI metadata 测试
│   │   └── test_scene_state.py                       scene 事实流测试：只注入三类事实、目标可选中端点，以及目标数区间端点显式归位（含相邻同目标数区间不来回翻转、事实流重放与查表一致）
│   ├── training/                                     训练模块测试子目录
│   │   ├── __init__.py                               子包入口
│   │   ├── _common_fixtures.py                       训练公共层测试共享的 PT、compiled cache 和演示战斗构造辅助；训练样本经 C# 状态机后端构建
│   │   ├── test_attention_residual.py                Full AttnRes 的均匀初始化与 pseudo-query 反向传播测试
│   │   ├── test_candidate_order.py                   候选人工顺序文件解析、候选 aligned 字段重排和 label 映射测试
│   │   ├── test_common_norm.py                       训练公共层归一化与 scene schema 测试，覆盖时间/威力/资源/状态上限和 scene 窗口索引
│   │   ├── test_common_pt.py                         训练公共层 PT、compiled cache、DataLoader 和 batch padding 测试
│   │   ├── test_common_vocab.py                      训练公共层 shard batch sampler 与序列过采样测试
│   │   ├── test_grouped_attention.py                 MQA/GQA 投影、头数配置、按 KV 组 SDPA、零 stride K/V 广播、mask/梯度和 trace 权重形状测试
│   │   ├── test_input_contract.py                    checkpoint 输入契约与归一化契约的序列化、恢复和缺失契约校验测试
│   │   ├── test_kv_cache.py                          模型推理 KV-Cache 测试，覆盖历史追加、候选状态变化、上下文重建、Pre-LN/Post-LN、Full AttnRes、训练模式禁用、配置 K/V 头数和 checkpoint 参数稳定性
│   │   ├── test_model_helpers.py                     公共模型与训练流程边界测试，覆盖配置错误、模型输入校验、split attention/FFN dropout、训练循环、DataLoader 分支、职业路由防护、价值辅助损失、checkpoint 落盘和断点续训
│   │   ├── test_models.py                            公共模型测试，覆盖 compiled cache 动态维度、职业路由、position 编码、训练精度配置、技能价值辅助损失、推理路径和脚本层副本目录比例选 raw JSON
│   │   ├── test_policy_actions.py                    独立 policy 动作配置与训练词表测试，覆盖 raw id 0 的 `ogcd_wait` 非 padding 映射、候选顺序及非法配置拒绝
│   │   ├── test_runtime_debug.py                     训练运行时诊断测试，覆盖 RuntimeDebugRecorder 的 batch 上下文写入、Attention/FFN 阶段记录与事件聚合
│   │   ├── test_swiglu.py                            SwiGLU 三投影门控公式、参数预算、GELU/ReLU 兼容性、激活配置 checkpoint 往返、全模型统一激活与旧 scorer 布局拒绝路径、融合快速路径回归测试
│   │   ├── test_tensorboard.py                       TensorBoard 配置校验、BC/GRPO 独立开关与共用事件根目录、启动器 `.env` 自动选择、可选依赖提示和标量过滤测试
│   │   ├── test_train_cli.py                         训练 CLI 直接执行入口和 `--resume` 参数转发测试
│   │   └── test_training_optimizations.py            split 分段融合、重复惩罚缓存、padding、指标累积和 checkpoint/梯度等价性测试
│   ├── __init__.py                                   测试包入口，显式声明 `tests` 为可导入包
│   ├── conftest.py                                   pytest 公共引导与共享夹具，保证项目根目录可稳定导入、隔离 Windows 临时目录权限并提供默认黑魔/机工状态机
│   ├── helpers.py                                    测试辅助函数模块，集中封装自动推进、向量取值、复用正式 scene token 构造器的场景上下文构造、canonical 输出断言和 YAML 写入
│   ├── test_main.py                                  根目录 CLI 回归测试，锁定 Sidecar 绝对时间命令入口
│   ├── test_policy_config.py                         策略配置清单测试，覆盖动作质量权重合并、相对路径解析与旧清单兼容
│   ├── test_torch_serialization.py                   根目录公共 PyTorch 安全文件读取测试，覆盖恶意 pickle 拒绝和旧 checkpoint Path 元数据兼容
│   └── test_yaml_config.py                           根目录公共 YAML mapping loader 测试，覆盖空文档/`null` 归一化和 falsey 非 mapping 顶层值校验
├── third_party/                                      外部项目源码目录，子模块版本由主仓库固定，我们的适配代码放在子模块外
│   └── xivanalysis/                                  MIT 许可的外部日志分析器 Git 子模块，用于后续离线动作质量标注；保留上游结构与 LICENSE
├── training/                                         预训练专属数据管线、训练循环与训练配置；不再承载 GRPO/回放/ONNX 共用的策略模型和输入数据组件
│   ├── config/                                       预训练运行配置、价值偏好配置和训练解析器
│   │   └── __init__.py
│   ├── data/                                         预训练数据管线目录，只消费最终 compiled cache，不负责 raw 转换或 cache 构建
│   │   ├── __init__.py                               预训练数据管线导出入口，仅暴露 dataset / collator / sampler / oversampling
│   │   ├── collator.py                               训练 collator，按显式 `history_bank_id` 合并 compact history 引用并完成 candidate / scene 的 batch padding
│   │   ├── dataset.py                                训练数据集，负责 cache schema 校验、skill vocab 映射、shard 读取、历史窗口裁剪和 structured tensor 输出
│   │   ├── oversampling.py                           可选职业 YAML 变长动作序列过采样规则加载器与样本权重构建器
│   │   └── sampler.py                                shard-aware batch sampler，按已编译缓存 shard 组织训练/验证 batch
│   ├── runtime/                                      训练运行时诊断
│   │   ├── __init__.py                               运行时诊断导出入口
│   │   └── runtime_debug.py                          batch、输入编码、Attention/FFN、反向和优化器阶段的显存/耗时诊断
│   ├── loop/                                         数据加载、训练循环和 checkpoint 生命周期
│   │   ├── __init__.py                               预训练训练流程导出入口
│   │   ├── checkpoint.py                             checkpoint 保存、续训契约校验、最佳指标、scheduler/RNG 恢复和安全加载
│   │   ├── dataloaders.py                            训练/验证文件拆分、normalizer/collator、过采样、shard sampler 和 DataLoader 构造
│   │   ├── training_loop.py                          训练初始化、optimizer/scheduler、混合精度 epoch、验证、TensorBoard batch/epoch 指标回调和 LR 调度
│   │   └── value_preference.py                       技能价值辅助排序损失，消费状态机输出的运行时 `candidate_values`
│   ├── __init__.py                                   训练包导出入口，仅暴露预训练 dataset / collator / sampler / oversampling 以及训练配置入口
│   └── train.py                                      通用行为克隆训练入口，支持 `python training/train.py` 和 `python -m training.train`
├── .coverage                                         coverage 本地产物文件，属于测试副产物，不参与项目实现
├── .env                                              本机环境变量覆盖文件，维护 FFLogs 路由、职业与模型变体、TensorBoard Web 端口、raw 数据根目录、compiled cache、模型 checkpoint、训练设备和回放解码参数等本地默认值，默认不提交
├── .env.example                                      项目根目录环境变量示例，统一约定训练、TensorBoard、回放、ONNX 导出与 parity 门禁参数；导出与回放共用模型路径和 Provider
├── .gitattributes                                    Git 文本属性配置，约束仓库里的换行和文本处理规则
├── .gitignore                                        Git 忽略规则，过滤缓存、临时文件和不应提交的本地内容
├── .gitmodules                                       Git 子模块路径和上游地址声明，记录 third_party/ 下的外部依赖
├── AGENTS.md                                         本地协作规范文件，记录开发约束、发布约定和项目架构说明
├── CHANGELOG.md                                      项目变更说明文件，记录当前重构后的里程碑和后续变更
├── ffxiv_ccg.ps1                                     根目录常用工具统一入口，提供中文编号菜单和 `-Action` 分步调用（训练、恢复训练、GRPO、ONNX 导出、模型分析、自回归回放、FFLogs 下载）；BC/GRPO 分别按所属 YAML 开关自动启动监控，业务参数读取 `config/` 与 `.env`
├── main.py                                           命令行入口，提供 `validate`、`list-skills`、`list-actions`、`smoke` 等最小手动验证命令；`list-actions` 和 `smoke` 通过进程内接口调用 C# 状态机
├── pytest.ini                                        pytest 配置文件，指定测试目录和缓存目录
├── README.md                                         项目命令说明文件
├── setup.ps1                                         一键准备项目 Python GPU 环境：检查系统 Python、创建或复用根目录 `.venv`、确保 pythonnet 安装于虚拟环境、安装依赖并验证 CUDA provider，不覆盖 `.env`
├── requirements-onnx-gpu.txt                         本地 NVIDIA CUDA 13.x ONNX 导出与 ORT GPU 回放依赖，与 CPU 版依赖二选一安装
├── requirements-onnx.txt                             独立 CPU ONNX 导出与 ONNX Runtime 校验依赖，不耦合训练主环境安装流程
├── requirements.txt                                  项目 Python 运行依赖与训练、TensorBoard、FFLogs 抓取及测试所需依赖
└── THIRD_PARTY_NOTICES.md                             第三方组件来源、使用范围及原始版权和许可文本
```
