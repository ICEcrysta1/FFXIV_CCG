# -*- coding: utf-8 -*-
# === 独立 ONNX 导出与运行验证（scripts/onnx_export）===
# 不并入训练主依赖；先安装 requirements.txt 中固定的 torch 2.12.0+cu132，
# 再在需要生成或校验 ONNX 产物时安装本文件，避免 torch/onnxscript 漂移。
onnx==1.22.0
onnxruntime==1.27.0
onnxscript==0.7.1
jsonschema==4.23.0
